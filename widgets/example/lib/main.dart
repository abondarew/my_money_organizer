import 'package:flutter/material.dart';
import 'package:widgets/card_with_avatar/card_with_avatar.dart';

void main() => runApp(
      MaterialApp(
        home: Scaffold(
          backgroundColor: Colors.grey.shade200,
          //body: AvatarCardBuilder.build(),
        ),
      ),
    );
